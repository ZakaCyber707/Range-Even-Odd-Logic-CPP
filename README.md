# Range Parity Checker (For Loop)


## 📊 Logic Overview
This project demonstrates the efficiency of combining **Loops** and **If-Else Statements** to process data sequences:

1. **Fixed Iteration:** The `for` loop is configured to run from `0` to `10`.
2. **Modulo Condition:** Inside each step, the program evaluates `i % 2 == 0`.
3. **Formatted Output:** Results are printed as a labeled list (e.g., `2:Even`).


## 🛠️ Features
* **Automated Classification:** Processes multiple numbers without requiring separate inputs.
* **Clean Formatting:** Uses string concatenation to display numbers alongside their classification.


 🚀 How to Run
Follow these steps to compile and execute the program on your local machine.

📋 Prerequisites
Ensure you have a C++ compiler installed:

Windows: MinGW or Visual Studio.
macOS: Xcode Command Line Tools (g++).
Linux: GCC (g++). 
